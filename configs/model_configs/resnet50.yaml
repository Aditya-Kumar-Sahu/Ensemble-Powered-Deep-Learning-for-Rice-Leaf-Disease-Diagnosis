# ResNet50 Model Configuration

model:
  name: "resnet50"
  architecture: "resnet"
  pretrained: false
  dropout: 0.0

training:
  learning_rate: 0.00005
  weight_decay: 0.0001

optimizer:
  name: "adam"

scheduler:
  name: "cosine"
  T_max: 15
