# MobileNetV2 Model Configuration

model:
  name: "mobilenetv2"
  architecture: "mobilenet"
  pretrained: false
  dropout: 0.2

training:
  learning_rate: 0.0001
  weight_decay: 0.0001

optimizer:
  name: "adam"

scheduler:
  name: "cosine"
  T_max: 15
