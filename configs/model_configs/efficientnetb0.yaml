---
# EfficientNet-B0 Model Configuration

model:
  name: "efficientnetb0"
  architecture: "efficientnet"
  pretrained: false
  dropout: 0.2

training:
  learning_rate: 0.0001
  weight_decay: 0.0001

optimizer:
  name: "adam"

scheduler:
  name: "cosine"
  T_max: 15
